"""PSS dynamic models.

* :mod:`pylectra.models.pss.none` — zero-state, zero-output PSS (the only
  model the legacy code shipped, now natively re-implemented), name
  ``"none"``.
"""
from . import none     # noqa: F401  (registers "none")
